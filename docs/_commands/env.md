---
layout: page
title: deploy4j env
---
Manage your environment files.

`deploy4j env push` uses secrets configuration in config/deploy.yml and env variables from .env to create and push env files to your hosts. Those files are passed to containers when they are booted.

See also [deploy4j envify](envify.md).

Example:

```shell

```