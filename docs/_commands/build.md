---
layout: page
title: deploy4j build
---
Pull app images to your servers. These commands are called indirectly by `deploy4j deploy` and `deploy4j redeploy`.

```shell
Usage: deploy4j build [--help] [COMMAND]
Build application image
      --help   Display help about a command
Commands:
  pull  Pull app image from registry onto servers
```