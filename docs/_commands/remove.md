---
layout: page
title: deploy4j remove
---
This will remove app, Gateway and accessory containers and log out of the Docker registry.

It will prompt for confirmation unless you add the -y option.

Examples:

```shell
```