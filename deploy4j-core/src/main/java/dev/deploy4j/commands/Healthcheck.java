package dev.deploy4j.commands;

import dev.deploy4j.Commander;
import dev.deploy4j.configuration.Configuration;

public class Healthcheck extends Base {
  public Healthcheck(Configuration config) {
    super(config);
  }
}
