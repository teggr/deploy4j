service: test-service
image: test/image

gateway:
  options:
    publish:
      - "443:443"
      - "80:80"
    volume:
      - "/etc/letsencrypt/acme.json:/etc/letsencrypt/acme.json"
      - "/var/log:/var/log"
    name: "gateway"
